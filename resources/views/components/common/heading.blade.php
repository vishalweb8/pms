<div class="page-title-box d-sm-flex align-items-center justify-content-between">
    @if($heading) <h4 class="mb-sm-0 font-size-18">{{ $heading }}</h4> @endif
    @if($btnText) <a href="{{ $btnUrl }}" class="btn btn-primary waves-effect waves-light">{{ $btnText }}</a> @endif
</div>
